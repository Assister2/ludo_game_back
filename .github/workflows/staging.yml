name: Build & Deploy
on:
  push:
    branches: [staging]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker buildx create --use
          docker buildx build --tag ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_STAG_REPO }} --file Dockerfile --push .
          docker buildx stop
        
      - name: Deploy Docker container to Droplet
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{secrets.SSH_HOST_STAGING_BACKEND_LUDO}} # IP address of the server you wish to ssh
          key: ${{secrets.SSH_KEY_STAGING_BACKEND_LUDO}} # Private or public key of the server
          username: ${{secrets.SSH_USERNAME_STAGING_BACKEND_LUDO}}
          script: |
            docker stop $(docker ps -q)
            docker rm $(docker ps -aq)
            docker rmi $(docker images -q)
            docker pull ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_STAG_REPO }}
            docker run -d -p 4001:4001 --name ${{ secrets.DOCKER_STAG_REPO }}-container --restart always ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_STAG_REPO }}
            echo 'Docker container deployed successfully'
